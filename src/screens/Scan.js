import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const Scan = () => {
  return (
    <View>
      <Text>scan</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Scan;
